# Filled from environment variables set by the GitHub Actions workflow / secrets.
app_identifier(ENV["APP_IDENTIFIER"] || "com.simplesettleapp.com")
apple_id(ENV["APPLE_ID"])           # your Apple Developer account email (optional with API key)
itc_team_id(ENV["ASC_TEAM_ID"])     # App Store Connect team id (optional)
team_id(ENV["DEVELOPMENT_TEAM"])    # Apple Developer (portal) team id
